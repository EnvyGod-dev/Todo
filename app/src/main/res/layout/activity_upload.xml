<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".UploadActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="50dp"
            android:layout_marginEnd="20dp"
            android:layout_marginStart="20dp"
            app:cardCornerRadius="30dp"
            app:cardElevation="10dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp"
                android:background="@drawable/lavender_border">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Шинээр оруулах"
                    android:textSize="24sp"
                    android:textAlignment="center"
                    android:textColor="@color/lavender"
                    android:layout_marginBottom="16dp" />

                <!-- ImageView for Upload -->
                <ImageView
                    android:id="@+id/uploadImage"
                    android:layout_width="match_parent"
                    android:layout_height="200dp"
                    android:src="@drawable/uploadimg"
                    android:layout_marginTop="10dp"
                    android:scaleType="fitCenter" />

                <!-- EditText for Title -->
                <EditText
                    android:id="@+id/uploadTopic"
                    android:layout_width="match_parent"
                    android:layout_height="60dp"
                    android:hint="Гарчиг оруулах"
                    android:background="@drawable/lavender_border"
                    android:padding="10dp"
                    android:textColor="@color/lavender"
                    android:layout_marginTop="16dp" />

                <!-- EditText for Description -->
                <EditText
                    android:id="@+id/uploadDesc"
                    android:layout_width="match_parent"
                    android:layout_height="100dp"
                    android:hint="Хийх зүйл"
                    android:background="@drawable/lavender_border"
                    android:padding="10dp"
                    android:textColor="@color/lavender"
                    android:layout_marginTop="16dp"
                    android:gravity="top|start" />

                <!-- EditText for Language -->
                <EditText
                    android:id="@+id/uploadLang"
                    android:layout_width="match_parent"
                    android:layout_height="60dp"
                    android:hint="Хэл оруулах"
                    android:background="@drawable/lavender_border"
                    android:padding="10dp"
                    android:textColor="@color/lavender"
                    android:layout_marginTop="16dp" />

                <!-- Save Button -->
                <Button
                    android:id="@+id/saveButton"
                    android:layout_width="match_parent"
                    android:layout_height="60dp"
                    android:text="Хадгалах"
                    android:textSize="18sp"
                    android:textColor="@android:color/white"
                    android:layout_marginTop="20dp"
                    android:backgroundTint="@color/lavender" />
            </LinearLayout>
        </androidx.cardview.widget.CardView>
    </LinearLayout>
</ScrollView>
